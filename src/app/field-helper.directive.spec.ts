import { FieldHelperDirective } from './field-helper.directive';

describe('FieldHelperDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldHelperDirective();
    expect(directive).toBeTruthy();
  });
});
